# 完了時チェックリスト

- 関連テストがすべてパスしていること（`pnpm test` / 必要に応じて `pnpm verify-order`）。
- Lint/Format が通っていること（`pnpm lint` / `pnpm format`）。
- 変更が既存パターンに沿っていること（DDD/CQRS/ES の文脈を崩さない）。
- CHANGELOG.md は編集しない（GitHub Actions が自動生成）。

updated_at: 2025-12-30
